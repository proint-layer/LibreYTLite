.theos/
.DS_Store
packages/*.deb
*.deb
*.ipa
*.ips
diff.txt
console
HANDOFF.md
YTClean/

# Local editor / clangd config (machine-specific absolute paths — keep out of the repo)
.vscode/
compile_flags.txt
